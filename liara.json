{
    "platform": "laravel",
    "app": "accfarm",
    "port": 8000,
    "phpVersion": "8.2",
    "buildCommand": "composer install --optimize-autoloader --no-dev && php artisan config:cache && php artisan route:cache && php artisan view:cache",
    "cron": [
        "30 00 * * * cd $ROOT && php artisan app:generate-daily-cycles >> /dev/null 2>&1"
    ],
    "static": false
}
